\IfDefined{sisetup}{%
    \sisetup{%
        detect-all,                  % Detecta el modo de la fuente
        range-phrase=--,             % Rango de números con guiones
        range-units=single,          % Solo una instancia de la unidad en rango
        separate-uncertainty=true,   % Separar incertidumbre
        multi-part-units=single,     % Unidad de múltiples partes
        per-mode=fraction,           % Modo de fracción
        table-alignment-mode=format, % Alineación de tablas
        table-format=2.4,           % Formato de tabla
        table-text-alignment=center, % Alineación de texto en tablas
        table-align-uncertainty=true,% Alineación de incertidumbre
        round-precision=3,           % Precision de redondeo
        round-mode=places,           % Modo de redondeo
    }
}